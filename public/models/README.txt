Place your GLB/GLTF ceramic cat mug here.

File path expected by app: /models/cat_mug.glb

Requirements:
- Prefer binary .glb
- Separate nodes named exactly: Mug, Saucer
- Export with tangents/normals for best visual quality
- Optionally include an HDRI for Environment if you want higher fidelity
